# File/storage operations use Django default_storage (MinioStorage).
# See edulife.storage and STORAGES["default"] in settings.
# For one-off bucket creation, use MinIO console or a management command.
